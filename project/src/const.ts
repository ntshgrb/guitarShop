export enum NameSpaces {
  guitars = 'GUITARS',
}

export enum APIRoute {
  Guitars = '/guitars',
}

export const MAX_GUITARS_COUNT = 9;

export enum AppRoute {
  Root = '/',
  Catalog = '/catalog',
  CatalogPage = 'catalog/',
}
